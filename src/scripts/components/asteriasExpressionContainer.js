/**
* factory function for getting an object
*
* @method AsteriasExpressionContainer
* @param {Object} store; the redux store that this container will listen to
* @param {Object} opt; configuration options for the container
*/
function asteriasExpressionContainer(store,opt) {
    //store setup the subscriber
    //setup the change Handler
    //change action dispatch 
}